# Profile-specific overrides for tests. Layered ON TOP of src/main/resources/application.properties
# (a file named application.properties here would shadow it instead), so only the values that
# would otherwise need a local .env are listed.
#
# Dummy signing key — 32 zero bytes, Base64. Never used outside tests.
jwt.secret=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=
spring.data.mongodb.uri=mongodb://localhost:27017/quickart-test

anthropic.api.key=test
gemini.api.keys=test
huggingface.api.token=test
replicate.api.token=test

cloudinary.cloud.name=test
cloudinary.api.key=test
cloudinary.api.secret=test
